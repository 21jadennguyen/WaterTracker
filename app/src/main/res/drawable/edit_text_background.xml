<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/card_background" /> <!-- Light gray background -->
    <corners android:radius="8dp" /> <!-- Rounded corners -->
    <stroke
        android:width="1dp"
        android:color="@color/primary_color" /> <!-- Border color -->
</shape>